({
	init : function(component, event, helper) {
        //I originally started to write this to get the user's name, 
        //but realized there's an easier way all on the page, 
        //so this does nothing. 
        //Leaving it in case someone wants to expand it in the future.
        /*console.log("Run");
        let userId = $A.get("$SObjectType.CurrentUser.Id");*/
	}
})